import 'package:fluro/fluro.dart';
import 'package:flutter/material.dart';
//import '../pages/home_page/webview.dart';

Handler webViewHandler = Handler(
  handlerFunc: (BuildContext ctx, Map<String, List<String>> params) {
//    final String url = params['url'].first;
//    final String statusBarColor = params['statusColor'].first;
//    final bool hideAppBar = params['hideAppBar'].first == 'true' ? true : false;
//    return WebView(url: url, statusBarColor: statusBarColor, hideAppBar: hideAppBar);
  }
);